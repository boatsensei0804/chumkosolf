# dev image: next dev hot reload (ใช้คู่กับ volume mount ใน docker-compose.yml)
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["npm", "run", "dev"]
